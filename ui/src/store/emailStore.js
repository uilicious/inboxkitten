
// State
const state = {
  email: 'tetest'
}

const mutations = {
  changeEmail: function (state, value) {
    state.email = value
  }
}

const getters = {}

const actions = {}

export default {
  mutations,
  state,
  getters,
  actions
}
